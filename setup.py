from setuptools import setup, find_packages

setup(
    name="CalcModule",
    version="0.1.0",
    description="An advanced calculation module for Python.",
    author="DrIncognito",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)